%ul
  - @categories.each do |category|
    %li= link_to category.name, category_url(category)
    %ul
      - category.products.each do |p|
        %li= link_to p.name, product_url(p) 
        =link_to(image_tag(p.photo.url(:thumb)), p.photo.url(:original))